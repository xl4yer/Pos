@page "/inventory"
@attribute [StreamRendering(true)]
@rendermode InteractiveServer
@inject NavigationManager Nav
@inject ILocalStorageService Loc
@using System.Net.Http.Headers
@inject ISnackbar SnackBar
@inject IDialogService Dialog
@using DocumentFormat.OpenXml
@using DocumentFormat.OpenXml.Packaging
@using DocumentFormat.OpenXml.Spreadsheet
@inject IJSRuntime IJS

<PageTitle>Inventory Report</PageTitle>

<style type="text/css">
    .centered {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 80vh;
    }
</style>

<div class="container mt-5">
    @if (q == null || q.Length == 0)
    {
        <div class="centered">
            <div class="loader"></div>
        </div>
    }
    else
    {
        <MudTable Class="ma-5" Dense="true" Hover="true" Items="@q" HorizontalScrollbar="true" FixedHeader="@fixed_header" FixedFooter="@fixed_footer" Height="@(fixed_header || fixed_footer ? "455px" : "")">
            <ToolBarContent>
                <MudIconButton Icon="@Icons.Material.Filled.Refresh" @onclick="Refresh" Variant="Variant.Filled" Color="MudBlazor.Color.Tertiary" Size="Size.Small"/>
                <MudSpacer />
                <MudButton Variant="Variant.Filled" Color="MudBlazor.Color.Success" Size="Size.Small" @onclick="ExportToExcel">Export to Excel</MudButton>
            </ToolBarContent>
            <HeaderContent>
                <MudTh>
                    <MudTableSortLabel SortBy="new Func<Qty, object>(c => c.date)" InitialDirection="SortDirection.Descending">
                        Date
                    </MudTableSortLabel>
                </MudTh>
                <MudTh>Product Code</MudTh>
                <MudTh>Product Name</MudTh>
                <MudTh>Qty</MudTh>
            </HeaderContent>
            <RowTemplate>
                <MudTd>@context.date</MudTd>
                <MudTd>@context.code</MudTd>
                <MudTd>@context.name</MudTd>
                <MudTd>@context.qty</MudTd>

            </RowTemplate>
        </MudTable>

    }
</div>



@code {
    private Qty[]? q;
    bool fixed_header = true;
    bool fixed_footer = false;

    private async Task Refresh()
    {
        await GetQty();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {

        if (firstRender)
        {
            await GetQty();
            StateHasChanged();
        }
    }

    private async Task ExportToExcel()
    {
        using MemoryStream ms = new MemoryStream();
        using (SpreadsheetDocument spreadsheetDocument = SpreadsheetDocument.Create(ms, SpreadsheetDocumentType.Workbook))
        {
            WorkbookPart workbookPart = spreadsheetDocument.AddWorkbookPart();
            workbookPart.Workbook = new Workbook();

            WorksheetPart worksheetPart = workbookPart.AddNewPart<WorksheetPart>();
            var sheetData = new SheetData();
            worksheetPart.Worksheet = new Worksheet(sheetData);

            // Add header row
            Row headerRow = new Row();
            string[] headers = { "Date", "Product Code", "Product Name","Qty"};
            foreach (var header in headers)
            {
                headerRow.Append(new Cell
                    {
                        DataType = CellValues.String,
                        CellValue = new CellValue(header)
                    });
            }
            sheetData.Append(headerRow);

            // Add data rows
            foreach (var product in q)
            {
                Row dataRow = new Row();

                dataRow.Append(
                    new Cell { DataType = CellValues.String, CellValue = new CellValue(product.date.ToString("yyyy-MM-dd")) },
                    new Cell { DataType = CellValues.String, CellValue = new CellValue(product.code) },
                    new Cell { DataType = CellValues.String, CellValue = new CellValue(product.name) },
                    new Cell { DataType = CellValues.Number, CellValue = new CellValue(product.qty.ToString()) }
                );

                sheetData.Append(dataRow);
            }

            Sheets sheets = workbookPart.Workbook.AppendChild(new Sheets());
            Sheet sheet = new Sheet()
                {
                    Id = workbookPart.GetIdOfPart(worksheetPart),
                    SheetId = 1,
                    Name = "InventoryReport"
                };
            sheets.Append(sheet);

            workbookPart.Workbook.Save();
        }

        // Export file to browser
        var base64 = Convert.ToBase64String(ms.ToArray());
        await IJS.InvokeVoidAsync("downloadFile", "InventoryReport.xlsx", base64);
    }

    private async Task GetQty()
    {
        try
        {
            using (var client = new HttpClient())
            {
                client.BaseAddress = new Uri($"{Nav.BaseUri}");
                var response = await client.GetAsync($"api/Qty/GetQty");
                if (response.IsSuccessStatusCode)
                {
                    await Task.Delay(1000);
                    var contentResponse = await response.Content.ReadAsStringAsync();
                    q = JsonConvert.DeserializeObject<Qty[]>(contentResponse);
                    StateHasChanged();
                }
            }
        }
        catch (Exception ex)
        {
            SnackBar.Add($"Error: {ex.Message}", Severity.Error);
            StateHasChanged();
        }
    }
}